---
import BackToTop from "@components/control/BackToTop.astro";
import Footer from "@components/Footer.astro";
import Navbar from "@components/Navbar.astro";
import SideBar from "@components/widget/SideBar.astro";
import type { MarkdownHeading } from "astro";
import { Icon } from "astro-icon/components";
import lxgwWenKaiScreenCssUrl from "lxgw-wenkai-screen-webfont/lxgwwenkaiscreen.css?url";
import ImageWrapper from "../components/misc/ImageWrapper.astro";
import WaveDivider from "../components/misc/WaveDivider.astro";
import TOC from "../components/widget/TOC.astro";
import { siteConfig } from "../config";
import {
	BANNER_HEIGHT,
	BANNER_HEIGHT_EXTEND,
	MAIN_PANEL_OVERLAPS_BANNER_HEIGHT,
} from "../constants/constants";
import {
	getBannerFallbackSource,
	getRandomBannerConfig,
	pickBannerSource,
} from "../utils/banner-utils";
import { url } from "../utils/url-utils";
import Layout from "./Layout.astro";

interface Props {
	title?: string;
	banner?: string;
	description?: string;
	lang?: string;
	setOGTypeArticle?: boolean;
	headings?: MarkdownHeading[];
	dynamicToc?: boolean;
}

const {
	title,
	banner,
	description,
	lang,
	setOGTypeArticle,
	headings = [],
	dynamicToc = false,
} = Astro.props;
const hasBannerCredit =
	siteConfig.banner.enable && siteConfig.banner.credit.enable;
const hasBannerLink = !!siteConfig.banner.credit.url;

const mainPanelTop = siteConfig.banner.enable
	? `calc(${BANNER_HEIGHT}vh - ${MAIN_PANEL_OVERLAPS_BANNER_HEIGHT}rem)`
	: "5.5rem";

const fallbackBannerSource = getBannerFallbackSource(siteConfig.banner.src);
const pickedFallbackBanner = pickBannerSource(fallbackBannerSource);
let bannerCreditText = siteConfig.banner.credit.text;
let bannerCreditUrl = siteConfig.banner.credit.url;
const fallbackBannerImage = pickedFallbackBanner.src;
// 如果横幅是一个数组，我们需要随机选择一个，并选择相应的信用文本和url
if (pickedFallbackBanner.index !== null) {
	const index = pickedFallbackBanner.index;
	if (Array.isArray(bannerCreditText)) {
		bannerCreditText = bannerCreditText[index];
	}
	if (Array.isArray(bannerCreditUrl)) {
		bannerCreditUrl = bannerCreditUrl[index];
	}
}

const bannerImage =
	banner && banner.trim().length > 0 ? banner : (fallbackBannerImage ?? "");
const hasBannerImage = bannerImage.length > 0;
const bannerText = bannerCreditText as string;
const bannerUrl = bannerCreditUrl as string;
const randomBannerConfig = getRandomBannerConfig(siteConfig.banner.src);
const randomBannerEnabled =
	siteConfig.banner.enable &&
	!!randomBannerConfig?.api &&
	!!randomBannerConfig.ossBase;
const normalizePath = (path: string) =>
	path
		.replace(/^https?:\/\/[^/]+/i, "")
		.split(/[?#]/)[0]
		.replace(/^\/|\/$/g, "")
		.toLowerCase();
const normalizedBase = normalizePath(url("/"));
const isHomeListPagePath = (path: string) => {
	const normalizedPath = normalizePath(path);
	if (normalizedPath === normalizedBase) return true;
	const relativePath =
		normalizedBase && normalizedPath.startsWith(`${normalizedBase}/`)
			? normalizedPath.slice(normalizedBase.length + 1)
			: normalizedPath;
	return /^\d+$/.test(relativePath);
};
const isHomePage = isHomeListPagePath(Astro.url.pathname);
const bannerWrapperTop = `calc(-1 * var(--banner-wrapper-offset, var(--banner-height-extend, ${BANNER_HEIGHT_EXTEND}vh)))`;
---

<Layout title={title} banner={banner} description={description} lang={lang} setOGTypeArticle={setOGTypeArticle}>

<!-- Navbar -->
<slot slot="head" name="head"></slot>
{isHomePage && <link slot="head" rel="stylesheet" href={lxgwWenKaiScreenCssUrl} />}
<div id="top-row" class="z-50 pointer-events-none relative h-[4.5rem] transition-all duration-700 max-w-[var(--page-width)] px-0 md:px-4 mx-auto" class:list={[""]}>
    <div id="navbar-wrapper" class="pointer-events-auto fixed left-0 right-0 top-0 z-50 mx-auto max-w-[var(--page-width)] px-0 md:px-4 transition-all">
        <Navbar></Navbar>
    </div>
</div>

<!-- Banner -->
{siteConfig.banner.enable && hasBannerImage && <div
	id="banner-wrapper"
	class={`absolute z-10 w-full transition duration-700 overflow-hidden`}
	style={`top: ${bannerWrapperTop}`}
	data-random-banner-enabled={randomBannerEnabled ? "true" : "false"}
	data-random-banner-api={randomBannerConfig?.api}
	data-random-banner-oss-base={randomBannerConfig?.ossBase}
>
	<ImageWrapper id="banner" alt="Banner image of the blog" class:list={["object-cover h-full transition duration-700 opacity-0 scale-105"]}
                  src={bannerImage} position={siteConfig.banner.position}
    >
    </ImageWrapper>
	<WaveDivider class="z-20"></WaveDivider>
    <div class="hero-content absolute inset-0 flex flex-col items-center justify-center z-20 text-white pointer-events-none px-4">
        {isHomePage ? (
            <h1 class="text-4xl md:text-6xl font-bold tracking-widest mb-4 md:mb-6 drop-shadow-lg text-center" style="font-family: 'LXGW WenKai Screen', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', 'Noto Serif SC', serif;">{siteConfig.title}</h1>
        ) : (
            <div class="text-4xl md:text-6xl font-bold tracking-widest mb-4 md:mb-6 drop-shadow-lg text-center" style="font-family: 'LXGW WenKai Screen', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', 'Noto Serif SC', serif;">{siteConfig.title}</div>
        )}
        <div class="text-xl md:text-2xl opacity-90 tracking-wide drop-shadow-md text-center px-4 py-2 rounded-lg font-medium" style="font-family: 'LXGW WenKai Screen', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', 'Noto Serif SC', serif;">
            <span id="subtitle-typewriter" data-subtitle={siteConfig.homeSubtitle}></span>
        </div>
    </div>
    <div class="absolute bottom-28 md:bottom-20 w-full flex justify-center z-20 pointer-events-none">
        <button id="home-scroll-button" type="button" aria-label="Scroll to content" class="hero-content home-scroll-pearl text-white pointer-events-auto cursor-pointer border-0 bg-transparent p-0">
            <span class="home-scroll-pearl-line home-scroll-pearl-line-top" aria-hidden="true"></span>
            <span class="home-scroll-pearl-drop" aria-hidden="true"></span>
            <span class="home-scroll-pearl-line home-scroll-pearl-line-bottom" aria-hidden="true"></span>
        </button>
    </div>
	<!-- Banner image credit -->
	{hasBannerCredit && <a href={bannerUrl} id="banner-credit" target="_blank" rel="noopener" aria-label="Visit image source"
						   class:list={["group onload-animation transition-all absolute flex justify-center items-center rounded-full " +
						   "px-3 left-4 right-auto bottom-16 md:left-auto md:right-4 md:bottom-4 bg-black/60 hover:bg-black/70 h-9 z-30", {"hover:pr-9 active:bg-black/80": hasBannerLink}]}
	>
		<Icon class="text-white/75 text-[1.25rem] mr-1" name="material-symbols:copyright-outline-rounded" ></Icon>
		<div class="text-white/75 text-xs">{bannerText}</div>
		<Icon class:list={["transition absolute text-[oklch(0.75_0.14_var(--hue))] right-4 text-[0.75rem] opacity-0",
			{"group-hover:opacity-100": hasBannerLink}]}
			  name="fa6-solid:arrow-up-right-from-square">
		</Icon>
	</a>}
</div>}

<style is:global>
	.enable-banner:not(.is-post):not(.is-bangumi) #banner-wrapper .wave-divider {
		display: none;
	}
	.enable-banner.is-post #banner-wrapper .wave-divider {
		opacity: 0;
		transform: translateY(8px);
		transition: opacity 0.6s ease-out, transform 0.6s ease-out;
	}
	html.post-wave-divider-ready .enable-banner.is-post #banner-wrapper .wave-divider {
		opacity: 1;
		transform: translateY(0);
	}
	@media (max-width: 767px) {
		.enable-banner #banner-wrapper .wave-divider {
			display: none;
		}
	}
    .hero-content {
        opacity: 0;
        transition: opacity 0.7s ease-in-out;
        transform: translateY(20px);
    }
    .is-home .hero-content {
        opacity: 1;
        transform: translateY(0);
    }
    .home-scroll-pearl {
        position: relative;
        display: flex;
        width: 5.75rem;
        height: 7.25rem;
        align-items: center;
        justify-content: center;
        filter: drop-shadow(0 0.85rem 1.35rem rgb(0 0 0 / 0.38));
        outline: none;
    }
    .home-scroll-pearl::before {
        content: "";
        position: absolute;
        width: 3.5rem;
        height: 3.5rem;
        border-radius: 999px;
        background: radial-gradient(circle, rgb(255 255 255 / 0.34), rgb(255 255 255 / 0.08) 48%, rgb(255 255 255 / 0) 72%);
        animation: home-scroll-pearl-glow 2.8s ease-in-out infinite;
    }
    .home-scroll-pearl-line {
        position: absolute;
        left: 50%;
        width: 2px;
        transform: translateX(-50%);
        border-radius: 999px;
        background: linear-gradient(to bottom, rgb(255 255 255 / 0), rgb(255 255 255 / 0.9), rgb(255 255 255 / 0));
        transition: opacity 0.25s ease, height 0.25s ease;
    }
    .home-scroll-pearl-line-top {
        top: 0.35rem;
        height: 2.6rem;
    }
    .home-scroll-pearl-line-bottom {
        bottom: 0.25rem;
        height: 1.9rem;
        opacity: 0.68;
    }
    .home-scroll-pearl-drop {
        position: relative;
        width: 2.15rem;
        height: 2.15rem;
        border-radius: 999px;
        background:
            radial-gradient(circle at 35% 28%, rgb(255 255 255 / 0.96) 0 18%, rgb(255 255 255 / 0) 19%),
            linear-gradient(135deg, rgb(255 255 255 / 0.92), rgb(222 236 248 / 0.58));
        box-shadow:
            inset 0 1px 1px rgb(255 255 255 / 0.7),
            inset 0 -0.42rem 0.85rem rgb(255 255 255 / 0.2),
            0 0.5rem 1.45rem rgb(0 0 0 / 0.32),
            0 0 1.15rem rgb(255 255 255 / 0.3);
        animation: home-scroll-pearl-fall 2.8s cubic-bezier(0.42, 0, 0.2, 1) infinite;
        transition: transform 0.25s ease, box-shadow 0.25s ease;
    }
    .home-scroll-pearl:hover .home-scroll-pearl-drop,
    .home-scroll-pearl:focus-visible .home-scroll-pearl-drop {
        transform: translateY(0.18rem) scale(1.05);
        box-shadow:
            inset 0 1px 1px rgb(255 255 255 / 0.78),
            inset 0 -0.42rem 0.85rem rgb(255 255 255 / 0.26),
            0 0.6rem 1.65rem rgb(0 0 0 / 0.34),
            0 0 1.35rem rgb(255 255 255 / 0.36);
    }
    .home-scroll-pearl:hover .home-scroll-pearl-line,
    .home-scroll-pearl:focus-visible .home-scroll-pearl-line {
        opacity: 0.9;
    }
    .home-scroll-pearl:focus-visible {
        border-radius: 999px;
        box-shadow: 0 0 0 2px rgb(255 255 255 / 0.68);
    }
    @keyframes home-scroll-pearl-fall {
        0%, 100% {
            opacity: 0.88;
            transform: translateY(-0.55rem) scale(0.96);
        }
        45% {
            opacity: 1;
            transform: translateY(0.28rem) scale(1);
        }
        70% {
            opacity: 0.94;
            transform: translateY(0.56rem) scale(0.98);
        }
    }
    @keyframes home-scroll-pearl-glow {
        0%, 100% {
            opacity: 0.45;
            transform: scale(0.9);
        }
        48% {
            opacity: 0.9;
            transform: scale(1.08);
        }
    }
    @media (prefers-reduced-motion: reduce) {
        .home-scroll-pearl::before,
        .home-scroll-pearl-drop {
            animation: none;
        }
    }
    #subtitle-typewriter::after {
        content: "|";
        animation: blink 1s step-end infinite;
    }
    #subtitle-typewriter.typing-done::after {
        display: none;
    }
    @keyframes blink {
        0%, 100% { opacity: 1; }
        50% { opacity: 0; }
    }
</style>

<script>
	import { setupHomeHero } from "../scripts/home-hero";
	import { setupRandomBanner } from "../scripts/random-banner";

	setupHomeHero();
	setupRandomBanner();
</script>

<!-- Main content -->
<div class="absolute w-full z-30 pointer-events-none" style={`top: var(--main-panel-top, ${mainPanelTop})`}>
    <!-- The pointer-events-none here prevent blocking the click event of the TOC -->
    <div class="relative max-w-[var(--page-width)] mx-auto pointer-events-none">
        <div id="main-grid" class="transition duration-700 w-full left-0 right-0 grid grid-cols-[17.5rem_auto] grid-rows-[auto_1fr_auto] lg:grid-rows-[auto]
    mx-auto gap-4 px-0 md:px-4 pointer-events-auto"
        >
            <SideBar class="mb-4 row-start-2 row-end-3 col-span-2 lg:row-start-1 lg:row-end-2 lg:col-span-1 lg:max-w-[17.5rem] onload-animation" headings={headings}></SideBar>

            <main id="swup-container" class="transition-swup-fade col-span-2 lg:col-span-1 overflow-hidden">
                <div id="content-wrapper" class="onload-animation">
                    <!-- the overflow-hidden here prevent long text break the layout-->
                    <!-- make id different from windows.swup global property -->
                    <slot></slot>
                    <div class="footer col-span-2 onload-animation hidden lg:block">
                        <Footer></Footer>
                    </div>
                </div>
            </main>

            <div class="footer col-span-2 onload-animation block lg:hidden">
                <Footer></Footer>
            </div>
        </div>

        <div class="pointer-events-auto">
            <BackToTop></BackToTop>
        </div>
    </div>
</div>

<!-- The things that should be under the banner, only the TOC for now -->
<div class="absolute w-full z-0 hidden 2xl:block">
    <div class="relative max-w-[var(--page-width)] mx-auto">
        <!-- TOC component -->
        {siteConfig.toc.enable && <div id="toc-wrapper" class:list={["hidden lg:block transition absolute top-0 -right-[var(--toc-width)] w-[var(--toc-width)] items-center", {"toc-empty": headings.length === 0}]}
        >
            <div id="toc-inner-wrapper" class="fixed w-[var(--toc-width)] overflow-y-scroll overflow-x-hidden hide-scrollbar">
                <div id="toc" class="w-full h-full transition-swup-fade ">
                    <div class="h-8 w-full"></div>
                    <TOC headings={headings} dynamic={dynamicToc}></TOC>
                    <div class="h-8 w-full"></div>
                </div>
            </div>
        </div>}

        <!-- #toc needs to exist for Swup to work normally -->
        {!siteConfig.toc.enable && <div id="toc"></div>}
    </div>
</div>
</Layout>
