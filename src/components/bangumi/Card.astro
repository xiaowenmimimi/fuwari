---
import type { UserSubjectCollection } from "@/types/bangumi";
import { renderBangumiCardHtml } from "@/utils/bangumi-render";

interface Props {
	item: UserSubjectCollection;
}

const { item } = Astro.props;
const cardHtml = renderBangumiCardHtml(item);
---

<Fragment set:html={cardHtml} />
